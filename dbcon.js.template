var mysql = require('mysql');
var pool = mysql.createPool({
  connectionLimit : 10,
  host            : 'localhost',
  user            : '<user>',
  password        : '<password>',
  database        : '<database_name>'
});

module.exports.pool = pool;
